export default function PsychologistLayout({ children }: { children: React.ReactNode }) {
  // Layout público para la ruta /psychologist; las rutas protegidas se manejarán en un grupo (protected)
  return children
}
